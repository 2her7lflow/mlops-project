# knowledge_base

## Structure
- `raw/` Source documents (PDF/CSV) for indexing
- `processed/` Generated vectorstores + chunks/pages + eval/manifest
- `feedback/` Exported user feedback (jsonl)

The backend uses `KNOWLEDGE_BASE_DIR` to locate this folder.
