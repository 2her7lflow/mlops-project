# eval/configs/ — Config files (optional)

`eval_config.yaml` is a placeholder for future improvements (e.g., running a whole grid of prompts/retrievers from one config).
If you don’t need it, you can ignore this folder.
